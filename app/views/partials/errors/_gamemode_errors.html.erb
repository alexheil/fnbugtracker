<% if @gamemode.errors.any? %>
  <div id="error_explanation">
      <h2><%= pluralize(@gamemode.errors.count, "error") %> prevented your gamemode from being saved:</h2>
    <ul>
    <% @gamemode.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>